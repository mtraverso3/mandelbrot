[package]
name = "mandelbrot-web"
version = "0.1.0"
edition = "2024"
license = "MIT"
publish = false

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
mandelbrot = { path = "..", default-features = false }
wasm-bindgen = "0.2"
